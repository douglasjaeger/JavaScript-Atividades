// array com 10 elementos utilizando o laço for
const letras = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j'];
console.log("array com 10 elementos utilizando o laço for")

for(let i = 0; i < letras.length; i++){
    console.log(letras[i]);
}